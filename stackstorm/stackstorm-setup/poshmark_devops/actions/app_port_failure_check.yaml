---
name: app_port_failure_check
runner_type: mistral-v2
description: "Log when app server port check fails and page on-call when it happens 10+ times in 60 mins"
enabled: true
entry_point: "workflows/app_port_failure_check.yaml"
parameters:
  msg:
    type: string
    description: "Body of the Alert"
    required: true
  host: 
    type: string
    description: "Hostname of app port failure"
    required: true
