---
name: check_datadog_agent
runner_type: mistral-v2
description: "Check status off datadog agent and restart if needed else send integration errors to Devops"
enabled: true
entry_point: "workflows/check_datadog_agent.yaml"
parameters:
  color:
    type: string
    description: "Slack Message color"
    required: true
  host: 
    type: string
    description: "Body of the Alert"
    required: true
